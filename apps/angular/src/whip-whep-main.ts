import { bootstrapApplication } from '@angular/platform-browser';
import { WhipWhepHostComponent } from './whip-whep/whip-whep-host.component';

bootstrapApplication(WhipWhepHostComponent).catch((error) => console.error(error));
