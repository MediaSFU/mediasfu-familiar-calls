# iOS evidence

The SwiftUI source and iOS-simulator type-check are complete. A physical-device
acceptance run still needs to capture the separate caller/recipient media,
screen-share priority, audio playout, and end/cleanup states before this folder
can claim live-room evidence.
