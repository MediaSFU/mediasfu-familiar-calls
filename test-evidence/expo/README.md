# Expo Web staging evidence

These public-safe images show the 2026-08-21 Expo Web acceptance run against a
real staging-created room through the repository backend. The host and guest
used separate browser origins and both reached the custom `Connected` call
surface. The ended-history screenshot confirms application-session teardown.

This is web evidence, not Android/iOS evidence. It does not claim camera,
microphone, remote playback, screen share, or native lifecycle acceptance; none
of those was observed during this run. See `manifest.json` for the exact gate.
